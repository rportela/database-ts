/**
 * A database key can only be a number or a string.
 * At least so far (at this point, no dates or arrays are allowed).
 *
 * @author Rodrigo Portela
 */
export type DatabaseKey = number | string;
